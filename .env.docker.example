POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Start infra: docker compose up -d postgres traefik nginx
# Gateway: http://localhost:8080/services/auth/health
# Host services: auth :9000, room :9001, realtime :9002 (npm run dev:auth / dev:room / dev:realtime)
# Socket.io via gateway: http://localhost:8080  path /socket.io

# Postgres shell:
# docker exec -it design-flow-postgres psql -U design_flow -d design_flow